#include "Base.xcconfig"

// Provisioning profiles
// Comment out the lines below if using automatic code sign
PROVISIONING_PROFILE_SPECIFIER[config=Debug][sdk=*][arch=*] = Rostam VPN Development
PROVISIONING_PROFILE_SPECIFIER[config=Staging][sdk=*][arch=*] = Rostam VPN Development
PROVISIONING_PROFILE_SPECIFIER[config=MockRelease][sdk=*][arch=*] = Rostam VPN Development
PROVISIONING_PROFILE_SPECIFIER[config=Release][sdk=*][arch=*] = Rostam VPN Release
